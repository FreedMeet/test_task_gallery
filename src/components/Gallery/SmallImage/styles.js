import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imgContainer: {
    backgroundColor: "#2a9d8f",
  },
  imageOverlay: {
    position: "absolute",
    left: 0,
    right: 0,
    bottom: 0,
    top: 0,
  },
});
